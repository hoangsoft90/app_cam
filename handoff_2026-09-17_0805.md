# handoff_2026-09-17_0805.md — bàn giao cho phiên sau

> Viết tay thay cho skill `handoff` (skill đó chỉ user tự gọi được). Đọc file này + `working.md` +
> `checklist.md` + `next.md` là đủ ngữ cảnh làm việc tiếp.

## 1. Project là gì?

**app_cam** — hệ thống quản lý **đại lý cám** (feed dealer) trên ERPNext: bán cám theo **lứa nuôi**
(`Feed Batch`), theo dõi **nợ theo lứa** qua lớp phân bổ (`Batch Debt`), hạn mức tín dụng, trả hàng,
xác nhận nợ, cấn trừ vật nuôi, báo cáo, AI hỗ trợ. Repo này **chỉ chứa app** `apps/feed_dealer` +
artifacts OpenSpec + tooling triển khai — **không quản runtime ERPNext**.

- Plan nguồn: `.plan/plan_final*.md` (+ patches v2.1–v2.3), phases trong `.plan/phases/`,
  chuỗi prompt `.plan/prompts/00_PROMPT_CHAIN.md`
- OpenSpec change đang chạy: `openspec/changes/p0-feed-dealer-foundation/` (`openspec validate` → valid)
- Journals: `working.md`, `checklist.md`, `features.md`, `next.md`, `faq.md`, `LESSONS_LEARNED.md`,
  `EXIT_GATE_PHASE1.md`, `result_*.txt`, `handoff_*.md`

## 2. Môi trường & cách tới đích (QUAN TRỌNG NHẤT)

| Thành phần | Giá trị |
|---|---|
| Site thật | ERPNext **v16** (frappe 16.17.2 / erpnext 16.16.0), site `frontend`, chạy trên **Mac của user** bằng `frappe_docker` |
| REST | `ERPNEXT_URL` + `ERPNEXT_API_KEY`/`ERPNEXT_API_SECRET` từ `.env` (git-ignored) qua ngrok |
| Bench | MCP bridge sang Mac: `python3 .agent/bench.py --site frontend <cmd> [--capture\|--to-file P]` |
| Chờ lệnh dài | **`python3 .agent/bench_wait.py <log> <bench args>`** — poll tới khi log có `=== EXIT n ===` rồi mới trả về. **Bắt buộc dùng khi chạy nhiều lệnh liên tiếp** (xem §5, bẫy race) |
| Sync app | `python3 .agent/push_to_mac.py apps/feed_dealer /Users/hoang/htdocs/erpnext/app_cam feed_dealer` (content-verified) |
| Xoá file | `rm -f` bị allowlist chặn — dùng `find <path> -delete` hoặc `python3 -c "import os; os.unlink('<path>')"`. `push_to_mac.py` KHÔNG xoá file chỉ có trên Mac (phải xoá tay) |
| MCP bridge | dựng lại bằng `python3 .agent/akimcp.py export` (đọc `AKI_*` từ `.env`) rồi `source /tmp/aki_env.sh`; payload MCP gửi qua file (`/tmp/payload.json`) + curl `--resolve` ghim IPv4 |
| Generator | `python3 .agent/gen_feed_dealer.py` — sinh 19 DocType JSON + controller + `--check` so byte-for-byte với đĩa. **Chạy `--check` trước mỗi push** |
| Báo cáo | `.agent/gen_reports.py` (7 report) + patch `v1_0/reimport_reports` |

- **Không được tự cài lại Docker ERPNext trên workspace** — đã xoá theo yêu cầu user (`alpine:3.19` là
  image có trước dự án, để nguyên).
- Bridge có thể đứt (host IPv6-only) → thử lại 1 lần; trong lúc chờ vẫn làm được qua REST ngrok.

## 3. Đã xong (bằng chứng đã capture)

| Phase | Trạng thái | Bằng chứng |
|---|---|---|
| P0 foundation | ✅ | 19 DocType (0 custom), migrate 2× `EXIT 0`, `p0_acceptance` **9/9** |
| P1A SI → Batch Debt | ✅ | `p1a_acceptance` **8/8** (idempotency theo bộ ba `(SI, batch, tax_template)`) |
| P1B PE → Payment Allocation FIFO | ✅ | `p1b_acceptance` **10/10** (cancel đảo ngược, row-lock chống over-allocate, cảnh báo `references`) |
| P1C Credit limit + Credit Score | ✅ | `p1c_acceptance` **10/10** (gate dùng chung `credit_limit.py`, override chỉ Manager) |
| P1D Sales Return | ✅ | `p1d_acceptance` **8/8** (gồm T8 regression do dataset P1G tìm ra) |
| P1F Legal/livestock/batch-ops | ✅ | `p1f_acceptance` **9/9** + mutation-check; 12 cột rác `tabBatch` đã drop (patch + backup) |
| P1G Reports + integrity + Exit Gate | ✅ | `P1G INTEGRITY` **9/9**, `P1G REPORTS` **4/4**, `EXIT_GATE_PHASE1.md` (5/7 PASS, Performance+UX NOT ASSESSABLE) |
| P1E e-invoice | ⛔ **BLOCKED** | cần provider (VNPT/Viettel/MISA) + credential sandbox + mã số thuế — xem `result_P1E_BLOCKED_2026-09-17.txt` |

Commit gần nhất (`git log -1 --format='%H %s'`):
```
121e29aa18967302db59df15a7ef718ff44073f4 docs: record the P1G commit hash (770be4e) in working.md and the result file
770be4e2f8f9b02d771c3d398c384717a416b027 feat(feed_dealer): P1G AR-vs-Batch-Debt integrity, 7 Desk reports, Phase 1 exit gate
```

## 4. Việc đang treo (làm tiếp khi vào phiên sau)

1. **[CHỜ USER QUYẾT] Cơ chế FIFO vs `references`** — số liệu thật: `ER 41.372.500` vs `PA 143.851.000`
   ⇒ gap **−102.478.500** (31/45 phiếu thu để trống `references`). Agent **KHÔNG tự sửa**; chờ user xem
   số rồi quyết. Đây là câu hỏi mở quan trọng nhất về nghiệp vụ.
2. **[CHỜ USER] Commit đợt docs review P1G** — working tree đang có thay đổi chưa commit (xem §7).
3. **[ ] Dọn dataset P1G** sau khi user review số thủ công:
   `bench execute feed_dealer.setup.p1g_integrity.cleanup`
4. **[ ] Tài khoản Desk thật + role `Driver`** — cần user cấp (hiện chỉ có user test `*-acceptance-*`).
5. **[ ] Ngưỡng + đo hiệu năng dữ liệu lớn** — mục Performance của Exit Gate đang NOT ASSESSABLE.
6. **[CHẶN GO-LIVE] P0.5 import nợ đầu kỳ** — `prompt_P0_5_migration.md` CHƯA LÀM. Không được go-live
   với dữ liệu khách thật khi chưa chạy: Batch Debt sẽ thiếu toàn bộ nợ lịch sử ⇒ mọi con số đối chiếu
   P1G lệch đúng phần đó.
7. **[ ] Archive change P0** khi user yêu cầu (`openspec-archive-change`), rồi mở change P0.5.

## 5. Bẫy đã biết (đừng dính lại — chi tiết `LESSONS_LEARNED.md` 1–46 + skill `.agents/skills/erpnext-v16-pitfalls`)

**Kiểm chứng (bài học mới nhất, quan trọng nhất):**
- **`--to-file` KHÔNG đảm bảo thứ tự** — gọi lệnh bench kế tiếp ngay sau đó là race; `debug` đọc DB khi
  `cleanup` còn xoá dở ⇒ `8/9` vô nghĩa. Dùng `.agent/bench_wait.py`.
- **Một suite đỏ ngay sau khi sửa code CHƯA chắc là regression.** Kiểm bằng chứng trước: số nào lẽ ra
  độc lập với thay đổi (PA/ER) mà **giống hệt** lần trước, còn nhóm liên quan về 0 ⇒ **dataset khác**,
  không phải logic khác. Chỉ sửa code sau khi tái lập được bằng chứng sạch.
- **Fixture phải kiểm bằng SỐ ĐÚNG** (hoá đơn/SO/phiếu thu/credit note) so với `shape` builder lưu, và
  `ensure_dataset` phải dọn dataset dở trước khi build lại — nếu không, rác từ build chết vẫn để suite xanh.
- Test "từ chối" phải chứng minh **không có gì được ghi** (fail-closed pre-write), không chỉ thấy exception.
- Đẳng thức tiền phải **tuyệt đối (0 đồng)** + kèm **mutation-check** (bỏ 1 số hạng → check phải đỏ đúng bằng số đó).

**Frappe v16 / hook:**
- Gate/guard phải ở `validate`/`before_submit` (chạy trước write), **không** `on_submit` (chạy sau khi đã ghi DB).
- `bench migrate` **bỏ qua standard doc khi `modified` không mới hơn** → dùng patch `frappe.reload_doc(..., force=True)`.
- Query Report **phải bắt đầu bằng `SELECT`/`WITH`** — comment `--` ở đầu bị `check_safe_sql_query` từ chối.
- `bench execute` nuốt exception thật → dùng `debug()` để in traceback thật.
- Tạo `UOM Conversion Factor` bắt buộc `category`; `UOM` v16 bỏ `conversion_factor`.
- Aggregate phải dict `[{"SUM": ...}]`; `doc.owner = X` trước insert không ăn → `db_set` sau insert.
- Xoá submitted doc phải `cancel()` trước; `frappe.get_hooks("doc_events")` có thể trả **list** (chỉ gọi handler app mình).
- Đặt tên DocType phải check trùng source `frappe`/`erpnext` (sự cố `Batch` đã xảy ra 1 lần).
- `Document.items.index(row)` so sánh **theo field** — dùng `enumerate` theo vị trí.

**Python:**
- Biến của list-comprehension có scope riêng — đừng dùng lại cùng tên làm biến vòng lặp (`UnboundLocalError`).
- Lỗi từ **app khác** (`custom_app` đang sửa song song) cũng làm test đỏ — đọc traceback trước khi nghi code mình.

## 6. Quy ước làm việc của user (bắt buộc giữ)

- **Chỉ tin "PASS" khi có bằng chứng dòng lệnh + output thật** — không báo xong bằng lời.
- Thao tác **không rollback** (DDL, xoá dữ liệu thật) và vùng **tài chính/phân quyền** → **đợi user duyệt**;
  việc không chặn & sửa được sau → tự quyết hướng an toàn nhất + ghi lý do vào `working.md`.
- **Clear cache trước mỗi lần test** cho chính xác; không dùng lại log cũ.
- Phụ thuộc bên ngoài (sandbox, tài khoản) mà tự tạo không được → **BLOCKED đúng cách, không chế mock**.
- Mỗi commit dán kèm output thật `git log -1 --format='%H %s'`, **không ghi hash từ trí nhớ**.
- Cập nhật `working.md`, `checklist.md`, `next.md`, `features.md`, `faq.md`, `result_*.txt` mới nhất,
  `LESSONS_LEARNED.md` + skill sau mỗi đợt việc.

## 7. Trạng thái working tree lúc bàn giao

Chưa commit (đợt docs review P1G): `openspec/.../design.md` (**D26**), `openspec/.../tasks.md` (**14c**),
`LESSONS_LEARNED.md` (42b–46), `checklist.md` (B0b), `features.md` (**1i**), `next.md` (**1c**),
`faq.md` (**10f/10g**), `result_2026-09-17_0805_review_P1G.txt`, `handoff_2026-09-17_0805.md`,
và 2 file code: `apps/feed_dealer/feed_dealer/setup/p1g_integrity.py`, `.../p1g_reports_check.py`.
Đã tái xác minh trên revision này: `P1G INTEGRITY 9/9` · `P1G REPORTS 4/4` · `P1D 8/8`.
